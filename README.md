# Ternforge r3 repository-control evidence

This repository contains only the OpenTofu source for the isolated r3 ruleset closure experiment. Local provider cache, lock file, state, and plan artifacts are ignored and must remain uncommitted.
